# AI-EDA Command Center: Dependencies

This frontend was built with Vite + React + TypeScript.

See the root `README.md` for full project documentation.

## Local Development

```bash
npm install
npm run dev
```

Open http://localhost:5173 after starting the Python backend (`python server.py` from the root).